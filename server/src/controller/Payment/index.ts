import { default as postTransferController } from './postTransferController/index.js';
import { default as postRedeemController } from './postRedeemController/index.js';

const controller = {
    postTransferController,
    postRedeemController
};

export default controller;
